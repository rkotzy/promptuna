import type { PromptunaConfig } from '../../src/config/types';

export const testConfigs: Record<string, PromptunaConfig> = {
  /* placeholder, real object will be re-exported from test-utils for now */
} as any;
